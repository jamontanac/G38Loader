[project]
name = "g38loader"
version = "0.1.0"
description = "GRCh38-anchored pipeline for downloading, verifying, and parsing ClinVar, gnomAD, OMIM, and other major genomic reference databases into structured JSON."
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "jmontana", email = "jmontana@n-ix.com" },
]
dependencies = [
    "pandas>=2.2",
    "pyyaml>=6.0",
]

[project.scripts]
download-databases = "g38loader.download:main"
verify-databases = "g38loader.verify:main"
databases-config = "g38loader.config_cli:main"
parse-clinvar = "g38loader.parse_clinvar:main"
parse-omim = "g38loader.parse_omim:main"
parse-equivalences = "g38loader.parse_equivalences:main"
parse-gnomad = "g38loader.parse_gnomad:main"

[dependency-groups]
dev = [
    "pytest>=8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/g38loader"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
